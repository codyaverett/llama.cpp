# Qwen3.6-40B-Deck-Opus-NEO-CODE-HERE — the big coder. Transcribed from serve-deckard.sh.
# 30890 MiB of weights, so it needs the whole rig: one shard, layer-split, pool is a pass-through.
MODEL="$MODELS_DIR/Qwen3.6-40B-Deck-Opus-NEO-CODE-HERE-2T-OT-Q6_K.gguf"
ALIAS=deckard
CARDS_PER_SHARD=8
CTX=32768               # ~+1.4 GB/card of KV across the rig; fits the 8 GB Vegas with margin
PARALLEL=1
CACHE_TYPE=
MMAP=off                # 31 GB model vs 31 GB RAM
ROUTING=ip_hash
EXTRA=""
